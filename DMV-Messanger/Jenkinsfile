pipeline {
    agent  none
    stages {
        stage('Deploy') {
            steps {
                 nodejs(nodeJSInstallationName: 'nodejs') {
                    sh 'npm --version'
                }
            }
        }
    }
}