pipeline {
    agent  any
    stages {
        stage('Deploy') {
            if (condition) {
                onIf
            } else {
                onElse
            }
            steps {
                 nodejs(nodeJSInstallationName: 'nodejs') {
                    sh '''npm --version
                          sls --version
                    '''

                }
            }
        }
    }
}