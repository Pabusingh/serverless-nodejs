pipeline {
    agent  any
    stages {
        stage('Deploy') {
            steps {
                 nodejs(nodeJSInstallationName: 'nodejs-latest-sls') {
                    sh '''npm --version
                          sls --version
                    '''

                }
            }
        }
    }
}